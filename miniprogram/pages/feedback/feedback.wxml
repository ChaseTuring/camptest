<!-- <view wx:for="{{list}}">
      <view>姓名</view>
      <input name="name" disabled="true" value="{{item.name}}"/>
      <view>qq号</view>
      <input name="qqnum" disabled="true" value="{{item.qqnum}}"/>
</view>
<view>
  <button bindtap="up" hidden="{{hid}}">上一页</button>
  <button bindtap="down" hidden="{{hid}}">下一页</button>
</view> -->

<!-- new -->
<view class="bg-white margin-top padding">
  <form class="container1" bindsubmit="onSub">
      <view class="cu-form-group">
      <textarea name="textarea"  placeholder="请输入意见..." maxlength="300"/>
    </view>
    <view class="padding flex flex-direction ">
    <button formType="submit" class="cu-btn bg-green lg">
      <view class="content" >Submit</view>
    </button>
  </view>

  <view class="padding flex flex-direction ">
    <button class="cu-btn bg-red lg" formType="reset">
      <view class="content" >Reset</view>
    </button>
  </view>
<view class="cu-bar margin-top"></view>
  </form>
</view>
